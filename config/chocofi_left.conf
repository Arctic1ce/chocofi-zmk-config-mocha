# Central half only. Do not set ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN or ZMK_DISPLAY_WORK_QUEUE_SYSTEM
# here — they clash with nice!view (custom screen + dedicated queue) and can fail the whole Kconfig step.

CONFIG_ZMK_WIDGET_LAYER_STATUS=y
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y
CONFIG_ZMK_WIDGET_WPM_STATUS=y
