# No extra central-only symbols here. nice!view already ships a custom status screen; enabling
# ZMK_WIDGET_LAYER_STATUS / OUTPUT_STATUS / WPM_STATUS duplicates LVGL code and commonly
# overflows FLASH on nice!nano + nice!view + BLE split.
